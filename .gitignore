# Secrets — never commit
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# Vendored third-party datasets — NOT committed.
# vendor/loughran_mcdonald is the LM Master Dictionary: commercial-licence-pending
# (see vendor/loughran_mcdonald/NOTICE.md). Committing licence-pending commercial data
# is redistribution. Fetch it at deploy time instead; core/lexicon.py loads it from
# vendor/ at runtime and degrades gracefully when it is absent.
vendor/

# OS junk
.DS_Store
Thumbs.db

# Runtime cache & local server logs (not source)
.nicegui/
server_restart.log
server_restart.log.err

# Generated document exports (produced on demand)
outputs/

# Marketing site now lives in its own repo: github.com/robbreza/irconnect-site
irconnect_live/
webheadshot.png
